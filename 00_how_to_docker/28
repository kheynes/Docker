docker service create --network overmind --replicas 2 --name marines -e OC_USERNAME=user -e OC_PASSWD=password 42school/marine-squad

#might change
docker service create --env OC_USERNAME=user --env OC_PASSWD=password --network overmind --replicas 2 --name marines 42school/marine-squad